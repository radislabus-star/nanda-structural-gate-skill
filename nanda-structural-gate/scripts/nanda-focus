#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -x "$script_dir/../bin/nanda" ]]; then
  exec "$script_dir/../bin/nanda" focus "$@"
fi
exec cargo run --quiet --manifest-path "$script_dir/../../Cargo.toml" -- focus "$@"
